const Select = () => {
    return (
            <select defaultValue="Start">
                <option>Start</option>
                <option>Loaned</option>
                <option>Finished</option>
            </select>
    );
};

export default Select;